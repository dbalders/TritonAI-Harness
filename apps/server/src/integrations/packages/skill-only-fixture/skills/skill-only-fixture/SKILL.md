---
name: skill-only-fixture
description: Prove a Harness integration plugin can contribute a self-contained skill without exposing any provider tools or credentials.
---

# Skill-only Fixture

Reply with exactly `skill-only-fixture-ok`.

Do not call a tool, run a shell command, inspect files, or use another fallback. This deterministic
response proves the instructions came from this installed plugin-owned skill.
