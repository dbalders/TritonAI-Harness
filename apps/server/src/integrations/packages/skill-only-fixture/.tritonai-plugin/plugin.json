{
  "apiVersion": "tritonai.harness/v2",
  "kind": "IntegrationPlugin",
  "manifestVersion": 2,
  "id": "skill-only-fixture",
  "name": "Skill-only Fixture",
  "description": "A test-only plugin proving Harness can install and run a plugin-owned skill with zero tools or credentials.",
  "version": "1.0.0",
  "capabilities": [
    {
      "id": "workflow.use",
      "displayName": "Use local workflow",
      "description": "Load the plugin-owned instructions. No service or data access is granted.",
      "access": "default"
    }
  ],
  "tools": [],
  "skills": [
    {
      "name": "skill-only-fixture",
      "description": "Prove a plugin-owned skill can run without any provider tool.",
      "capabilities": ["workflow.use"]
    }
  ]
}
